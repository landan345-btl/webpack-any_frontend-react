const UP: string = '升';
const DOWN: string = '降';
const FLAT: string = '平';
let isUpOrDown = (iNumber: number[]): any => {
  // 当前同位数 跟上一组同位数比大小
  //  iNumber[skey + 1].numbers) 上一组数据
  //  
  
};

export default isUpOrDown;
